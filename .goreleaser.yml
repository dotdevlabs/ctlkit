version: 2

before:
  hooks:
    - go mod tidy
    - go vet ./...

builds:
  # ctlkit is a library — no binary release.
  []

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
